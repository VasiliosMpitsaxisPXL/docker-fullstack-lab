# --- Database Configuration ---
POSTGRES_DB=appdb
POSTGRES_USER=appuser
# IN PROD: Use a secret store, do not rely on this file.
POSTGRES_PASSWORD=changeme

# --- Backend Configuration ---
BACKEND_PORT=3000

# --- Frontend Configuration ---
# In Dev: Nginx proxies /api to backend
# In Prod: Nginx serves frontend and proxies /api to backend
VITE_API_BASE_URL=/api

# Docker Hub username for pushing/pulling images
DOCKERHUB_USERNAME=yourusername
